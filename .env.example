# Senha da area administrativa (Etapa 4).
# Lida apenas no servidor — nunca use o prefixo NEXT_PUBLIC_ aqui.
ADMIN_PASSWORD=

# Segredo usado para assinar o cookie de sessao do admin (Etapa 4).
# Gere com: openssl rand -base64 32
ADMIN_SESSION_SECRET=

# Conexao do Neon Postgres (Etapa 6).
DATABASE_URL=
